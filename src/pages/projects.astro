---
import PublicLayout from "@/layouts/PublicLayout.astro";
import { Card, CardHeader, CardTitle, CardContent, CardFooter } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Github } from "lucide-react";

// Placeholder data for projects
const projects = [
  {
    title: "yangzhenblog-2025",
    description: "基于 Astro 5 + Cloudflare D1 + React 构建的现代化个人博客系统。",
    url: "https://github.com/spsz831/yangzhenblog",
    icon: "🚀"
  },
  {
    title: "Project Placeholder 1",
    description: "这是一个项目占位符。后续将替换为真实的 GitHub 项目信息。",
    url: "https://github.com/spsz831",
    icon: "📦"
  },
  {
    title: "Project Placeholder 2",
    description: "这是一个项目占位符。后续将替换为真实的 GitHub 项目信息。",
    url: "https://github.com/spsz831",
    icon: "🛠️"
  },
  {
    title: "Project Placeholder 3",
    description: "这是一个项目占位符。后续将替换为真实的 GitHub 项目信息。",
    url: "https://github.com/spsz831",
    icon: "✨"
  }
];
---

<PublicLayout title="项目">
  <section class="container max-w-6xl py-6 lg:py-10">
    <div class="flex flex-col items-start gap-4 md:flex-row md:justify-between md:gap-8">
      <div class="flex-1 space-y-4">
        <h1 class="inline-block font-heading text-4xl tracking-tight lg:text-5xl">
          项目
        </h1>
        <p class="text-xl text-muted-foreground">
          我的开源项目和实验性作品。
        </p>
      </div>
    </div>
    <hr class="my-8" />
    
    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-2">
      {projects.map((project) => (
        <Card className="h-full flex flex-col overflow-hidden border-muted-foreground/20 hover:border-primary/50 transition-colors">
          <CardHeader class="flex flex-row items-center gap-4 space-y-0">
            <div class="flex h-12 w-12 items-center justify-center rounded-lg bg-muted text-2xl">
              {project.icon}
            </div>
            <CardTitle className="text-xl">
              {project.title}
            </CardTitle>
          </CardHeader>
          <CardContent className="flex-1">
            <p class="text-muted-foreground leading-relaxed">
              {project.description}
            </p>
          </CardContent>
          <CardFooter className="flex gap-4">
            <Button variant="outline" size="sm" asChild>
              <a href={project.url} target="_blank">
                查看主页
              </a>
            </Button>
            <Button variant="ghost" size="sm" asChild>
              <a href={project.url} target="_blank">
                <Github className="mr-2 h-4 w-4" />
                Github
              </a>
            </Button>
          </CardFooter>
        </Card>
      ))}
    </div>
  </section>
</PublicLayout>
